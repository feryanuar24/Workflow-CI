name: hotel-booking

channels:
  - conda-forge

dependencies:
  - python=3.12.7

  - pip

  - pip:
      - mlflow==2.19.0
      - matplotlib
      - numpy
      - optuna
      - pandas
      - scikit-learn
      - xgboost
